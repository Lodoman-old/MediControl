{
  "name": "@medicontrol/brand",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tokens de marca y referencias de logo compartidos entre web y movil",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"no lint configured\"",
    "build": "echo \"source-only package, nothing to build\"",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "rimraf": "^6.0.1"
  }
}
